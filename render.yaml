# Render Blueprint: https://render.com/docs/blueprint-spec
services:
  - type: web
    name: barbell-dashboard
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run streamlit_app.py --server.port $PORT
